# aseprite-audio-video
This is an Aseprite extension for Linux that allows you to play animations with synced audio. It uses `ffplay` for background sound, and also export your animation as a video with the selected audio using `ffmpeg`.
